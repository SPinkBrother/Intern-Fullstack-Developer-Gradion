{
  "name": "gradion-book-studio",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run build -w @gradion/api && concurrently -n api,web -c blue,magenta \"node apps/api/dist/index.js\" \"npm run dev -w @gradion/web\"",
    "test": "npm run test -w @gradion/api && npm run test -w @gradion/web",
    "build": "npm run build -w @gradion/api && npm run build -w @gradion/web",
    "start": "npm run start -w @gradion/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
